{
  "name": "retailedge-backend",
  "version": "1.0.0",
  "description": "Backend for RetailEdge Lite - AWS Lambda functions",
  "main": "index.js",
  "scripts": {
    "deploy": "zip -r lambda.zip . && aws lambda update-function-code --function-name RetailEdgeSalesProcessor --zip-file fileb://lambda.zip",
    "test": "echo \"Run Python tests separately\""
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1483.0",
    "axios": "^1.4.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

